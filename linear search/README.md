Start from the leftmost element of arr[] and one by one compare target with each element of arr[]
If target matches with an element, return the index.
If target doesn’t match with any of elements, return -1.

